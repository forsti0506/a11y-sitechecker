#!/usr/bin/env node

// This file is the entry point if you are using it through the command line. Only 1 line as a wrapper

import { defaultFunction } from './a11y-sitechecker_code';

(async () => await defaultFunction())();
